# Cognitive Dungeon: Backend (Go)

Серверная часть проекта **Cognitive Dungeon** — текстовой онлайн RPG с гибридным ИИ, разработанная для проведения интерактивных ролевых игр. Сервер написан на **Go** и использует **WebSockets** для общения с клиентами в реальном времени.

## Архитектура

Сервер построен на принципах "авторитетного сервера", где вся игровая логика и физика обрабатываются на бэкенде, а клиенты выступают в роли тонких визуализаторов. Ключевые архитектурные решения:

-   **Единый реестр сущностей (Unified Entity Registry):** В игре нет жесткого разделения на "игрока" и "NPC". Все акторы (герои, монстры, союзники) являются `Entity` и хранятся в общем списке `GameService.Entities`. Это упрощает логику взаимодействия и таргетинга.

-   **Динамическое управление агентами (Dynamic Agent Control):** Сущность может управляться как человеком, так и ИИ. Переключение происходит "на лету":
    -   Когда клиент подключается через WebSocket и "завладевает" сущностью по её ID, игровой цикл переводит эту сущность в режим ожидания команд от человека.
    -   Если от сущности нет активного подключения (подписчика в `Hub`), игровой цикл автоматически обрабатывает её ход с помощью внутреннего серверного ИИ.
    -   Это позволяет игрокам подключаться и отключаться без остановки мира, а также дает возможность создавать внешних ботов-агентов, которые подключаются как обычные игроки.

-   **Компонентная система (ECS-like):** Сущности (`Entity`) состоят из компонентов (`StatsComponent`, `AIComponent`, `RenderComponent` и т.д.), что позволяет гибко определять их поведение и свойства.

## 🛠 Технологии
-   **Язык:** Go (Golang) 1.22+
-   **Протокол:** JSON over WebSockets
-   **Библиотеки:** `gorilla/websocket`

## 🚀 Запуск локально

1.  **Установите Go:** [Скачать Go](https://go.dev/dl/)
2.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Cognitive-Dungeon/cd-backend-go.git
    cd cd-backend-go
    ```
3.  **Установите зависимости:**
    ```bash
    go mod tidy
    ```
4.  **Запустите сервер:**
    ```bash
    go run cmd/server/main.go
    ```
    *Вы увидите сообщение: `🛡️ Cognitive Dungeon Server running on :8080`*

## 🔌 API (WebSocket)

**Адрес:** `ws://localhost:8080/ws`

Сервер использует асинхронный протокол на основе JSON. Клиент отправляет команды действий (`MOVE`, `ATTACK`) и получает в ответ полные обновления состояния мира (`UPDATE`).

Для получения полной информации о всех командах, структурах данных и потоке взаимодействия, пожалуйста, обратитесь к подробному справочнику:

### 📄 [**Полная документация по API (API.md)**](./API.md)

## 📂 Структура проекта
-   `cmd/server/main.go`: Точка входа, инициализация веб-сервера и обработчик WebSocket.
-   `internal/engine/`: Ядро игровой логики.
   -   `service.go`: Главный игровой сервис, управляющий игровым циклом (`Game Loop`).
   -   `handlers/`: Обработчики команд (`MOVE`, `ATTACK` и т.д.).
-   `internal/domain/`: Основные структуры данных игры (`Entity`, `GameWorld`, компоненты).
-   `internal/systems/`: Чистые функции (stateless), реализующие игровую механику (физика, бой, ИИ, поле зрения).
-   `internal/agent/`: Примеры "агентов" или ботов, которые могут подключаться к серверу.
-   `internal/network/`: Хаб для управления WebSocket-подписками (`Broadcaster`).
-   `pkg/`: Вспомогательные пакеты, не зависящие от основной логики (генератор подземелий, API-контракты).
-   `tools/`: Инструменты для отладки, включая веб-клиент.


